# EnerSight environment template — copy to .env and fill in.

# ----- API -----
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=True
ENVIRONMENT=development
LOG_LEVEL=INFO
LOG_FILE=logs/enersight.log

# ----- Supabase (backend) -----
# Project URL + keys: Supabase Dashboard → Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxx_or_legacy_anon_jwt
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxx
# JWT secret: Supabase Dashboard → Settings → API → JWT Settings → JWT Secret
SUPABASE_JWT_SECRET=replace-with-project-jwt-secret

# Postgres pooler connection string (port 6543 recommended for backend workloads).
# Format: postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_DB_PASSWORD@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# ----- Application -----
SECRET_KEY=change-me-to-a-long-random-value-of-at-least-32-chars
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ----- ML models -----
ML_MODELS_PATH=ml/models/trained
MODEL_REGRESSION=regression_random_forest.joblib
MODEL_LSTM=lstm_energy_forecast.keras
MODEL_ANOMALY=anomaly_detector.joblib

# ----- Frontend (Vite reads these at build) -----
VITE_API_URL=http://localhost:8000/api/v1
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx_or_legacy_anon_jwt
